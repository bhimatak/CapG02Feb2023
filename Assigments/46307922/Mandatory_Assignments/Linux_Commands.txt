1.	Pwd
2.	ls 
3.	a. mkdir d1
	b. mkdir -p d1/d2
	c. cd d2
	d. cat > f1.txt
	e. cat f1.txt
	f. ls ..
4.ls -d */
5.a. ls ?
  b. ls -la
  c. ls tb*.1
6.ls -S -la
7.rm temp*
8.head -5 file.txt
9.mkdir testdir
10. a. cp tb1.1 tbnew.1
    b. cp tb*.1 ./new/
    c.  mkdir -p new/new1
    d.  cp tb2.1 tb6.1 tb7.1 tb10.1 ./new/new1/
    e.  cp -R ./new/ ./newprogs/
11. a. mv: This command moves or rename files or directories.
       Cp: This command is used to copy files or directories.
	b. rm command is used to remove files from a directory. rmdir command is used to remove a directory if it is empty. We can also use rm with -r to
	   remove a directory which is not empty.
    c. mkdir: Used to make/ create a new directory.
       mkdir -p: checks and makes parent directory if does not exists.
12. rm -r testdir (or) rm-rdf testdir
13. man 
14. We could not change to a directory if the directory doesn’t exists or not created.
15. a. cd / - navigate to the root directory from the current directory.
    b. cd .. navigate to the parent directory from the present working directory.
	c. cd -navigate to home directory from the present working directory.
	d. cd ../.. – to change directory to multiple directory.

Advanced Optional Questions:
1.	ls command with -i displays the inode number of a file.
2.	Pipe command (|) is used to combine one command with another command.
3.	man ps – to find details over ps command.