1.pwd
2.ls
3. a. mkdir d1
    b.mkdir -p d1/d2
    c.cd d2
    d.cat f1.txt
    e.cat>f1.txt
    f.ls -1
4.ls -d*/
5. a. ls.?
   b.ls .* -la
   c.ls tb*.1
6.ls -S -la
7.rm temp*
8.head -5 text.txt
9.mkdir testdir
10.  a.cp tb1.1 tbnew.1
       b.cp tb*.1 ~/new/
       c.mkdir -p new/new1
       d.cp tb2.1 tb6.1
       e.cp -R /new//newprogs/
11.  a. mv:This command move or rename files or directories
            Cp:used to copy files or directories from one location or directories
          b.rm:used to remove a file from the directory
             rmdir:used to remove a directory.rmdir with -r deletes a non empty directory
         c.mkdir:used to make/create a new directory
            mkdir -p:the -p with the mkdir is used to create a directory and any parent directories that does not exist.
12.rm -rdf.testdir/
13.man
14.We could not change to a directory if the directory does not exists/not created
15. a.cd /: to navigate a directory to root directory from current working directory.
       b.cd ..:to navigate to the parent directory from present working directory
       c.cd:to change the directory
       d.cd../..:to change directory to multiple sub directory.
